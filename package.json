{
  "name": "night-neutralizer",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that reduces audio and video dynamic range for comfortable night-time viewing.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "node scripts/build.mjs --clean-only",
    "build": "node scripts/build.mjs",
    "build:dev": "node scripts/build.mjs --dev",
    "watch": "node scripts/build.mjs --dev --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "testpage": "node scripts/serve-test-page.mjs",
    "smoke": "node scripts/smoke.mjs",
    "zip": "node scripts/build.mjs && cd dist && zip -qr ../night-neutralizer.zip . && cd .. && echo \"wrote night-neutralizer.zip\"",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "0.2.2",
    "@types/node": "24.3.0",
    "esbuild": "0.28.1",
    "jsdom": "30.0.0",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
